{
    "name": "hybus-timetable",
    "private": true,
    "version": "1.0.0",
    "description": "timetable for hybus service",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'",
        "dev": "wrangler dev index.js --local",
        "deploy": "wranger publish index.js"
    },
    "author": "Taewan Park <swimingtw@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0",
        "wrangler": "^2.0.0"
    },
    "dependencies": {
        "itty-router": "^2.6.1"
    }
}
    